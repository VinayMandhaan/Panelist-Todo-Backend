# Panelist-Todo-Backend

### The server is developed using Node.js, Express.js and MongoDB.

### The server is deployed on Vercel

### To run the server, use the following commands.

* Clone the code
* Run yarn add
* Run yarn server
